/*
    Como compilar:
    
    Linux: g++ main.cpp -o main
*/

#include <iostream>

int main(){

    // std = Namespace || :: = acessa a função deste namespace || << operador que envia um dado para a função
    std::cout << "Hello World in C++!\n";

    return 0;
}