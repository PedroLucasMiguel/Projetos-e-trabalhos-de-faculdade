#include <iostream>
#include <array>
#include <vector>


int main(){

    std::vector<int> a;
    std::array<int, 20>; // Quase mesma coisa do vector, só que com valores limitados

    a.push_back(10);

    return 0;
}